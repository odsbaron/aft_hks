# 🎲 Sidebets - Social Consensus Betting on Monad

> Transform social debates into on-chain settlements through EIP-712 signature aggregation

## 🎯 项目概述

Sidebets 是一个去中心化的社交投注协议，允许用户创建预测市场，通过链下签名聚合达成共识，最终在 Monad 链上完成结算。

### 核心特性

| 特性 | 描述 |
|------|------|
| **零 Gas 认证** | 用户使用 EIP-712 签名参与，无需支付 Gas |
| **社交共识** | 参与者投票决定最终结果，阈值可配置 (50%-99%) |
| **争议机制** | 2 小时争议窗口，防止恶意结算 |
| **CREATE2 部署** | 可预测合约地址，便于前端集成 |
| **Monad 优化** | 并行执行友好，充分利用 Monad 高性能 |
| **社交登录** | 支持 Email/Google/Twitter/Discord/Telegram/Farcaster |
| **Telegram 集成** | Bot + Mini App 完整体验 |
| **自动结算** | Relayer 后端自动处理共识结算 |

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户层                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────────────┐         ┌──────────────────┐                    │
│   │   Web 浏览器      │         │   Telegram App   │                    │
│   │  (Next.js 前端)   │         │  (Bot + Mini App)│                    │
│   │  - 社交登录       │         │  - 市场发现      │                    │
│   │  - 市场管理       │         │  - 通知推送      │                    │
│   │  - 签名认证       │         │  - 快捷投注      │                    │
│   └────────┬─────────┘         └────────┬─────────┘                    │
│            │                            │                               │
└────────────┼────────────────────────────┼───────────────────────────────┘
             │                            │
             ▼                            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                            API 层                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────────────┐         ┌──────────────────┐                    │
│   │  Relayer 后端    │         │  Telegram Bot    │                    │
│   │  - 签名收集      │         │  - 命令处理      │                    │
│   │  - 自动结算      │◄────────┤  - 订阅管理      │                    │
│   │  - 市场同步      │         │  - 通知发送      │                    │
│   └────────┬─────────┘         └──────────────────┘                    │
│            │                                                          │
└────────────┼──────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        Monad Testnet                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌──────────────────┐         ┌──────────────────┐                    │
│   │ SidebetFactory   │────────▶│    Sidebet N     │                    │
│   │  (工厂合约)       │  CREATE2│   (市场合约)      │                    │
│   └──────────────────┘         │  - 资金托管      │                    │
│                                │  - EIP-712 验证  │                    │
│                                │  - 共识结算      │                    │
│                                └──────────────────┘                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 合约列表

| 合约 | 文件 | 功能 |
|------|------|------|
| `SidebetFactory` | `contracts/SidebetFactory.sol` | 创建和管理市场 |
| `Sidebet` | `contracts/Sidebet.sol` | 核心市场逻辑 |
| `MockToken` | `contracts/mocks/MockToken.sol` | 测试用 ERC20 代币 |
| `ISidebet` | `contracts/interfaces/ISidebet.sol` | 接口定义 |

## 📁 项目结构

```
aft/
├── packages/
│   ├── hardhat/                   # 智能合约开发
│   │   ├── contracts/
│   │   │   ├── interfaces/
│   │   │   │   └── ISidebet.sol   # 接口定义
│   │   │   ├── mocks/
│   │   │   │   └── MockToken.sol  # Mock USDC
│   │   │   ├── Sidebet.sol        # 核心市场合约
│   │   │   └── SidebetFactory.sol # 工厂合约
│   │   ├── deploy/
│   │   │   └── 01_deploy_sidebet_factory.ts
│   │   └── test/
│   │       └── Sidebet.test.ts    # 测试文件 (38个测试)
│   │
│   └── nextjs/                    # Web 前端
│       ├── app/
│       │   ├── page.tsx           # 主页
│       │   ├── markets/           # 市场列表
│       │   ├── create/            # 创建市场
│       │   ├── market/[address]/  # 市场详情
│       │   ├── api/               # API 路由
│       │   └── tg/                # Telegram Mini App
│       ├── components/
│       │   ├── sidebet/           # Sidebet 组件
│       │   │   ├── MarketCard.tsx
│       │   │   ├── ProgressBar.tsx
│       │   │   ├── StakeModal.tsx
│       │   │   ├── ProposeModal.tsx
│       │   │   ├── AttestationModal.tsx
│       │   │   ├── AttestationList.tsx
│       │   │   └── LoginModal.tsx
│       │   └── scaffold-eth/      # Scaffold-ETH 组件
│       ├── hooks/                 # React Hooks
│       │   ├── useSidebet.ts
│       │   ├── useSidebetFactory.ts
│       │   ├── useAttestation.ts
│       │   └── usePrivy.ts
│       ├── privy/                 # Privy 配置
│       └── contracts/
│           └── deployedContracts.ts
│
├── relayer/                       # 后端服务 (签名收集 + 自动结算)
│   ├── prisma/
│   │   └── schema.prisma          # 数据库 schema
│   ├── src/
│   │   ├── routes/                # API 路由
│   │   │   ├── attestations.ts
│   │   │   ├── markets.ts
│   │   │   └── health.ts
│   │   ├── services/              # 业务服务
│   │   │   ├── blockchain.ts
│   │   │   ├── signature.ts
│   │   │   └── finalization.ts
│   │   └── index.ts
│   └── README.md
│
├── telegram-bot/                  # Telegram Bot
│   ├── prisma/
│   │   └── schema.prisma
│   ├── src/
│   │   ├── handlers/              # 命令处理
│   │   ├── keyboards/             # 内联键盘
│   │   ├── middleware/            # 中间件
│   │   ├── services/              # 业务服务
│   │   └── index.ts
│   └── README.md
│
└── docs/                          # 文档
    ├── DEPLOYMENT.md              # 部署指南
    ├── MONAD_DEPLOYMENT.md        # Monad 部署
    ├── PRIVATE_KEY_SETUP.md       # 私钥配置
    └── TODO.md                    # 开发清单
```

## 🚀 快速开始

### 环境要求

- Node.js >= v20.18.3
- Yarn v1 或 v2+
- Git

### 安装与启动

```bash
# 1. 安装依赖
yarn install

# 2. 启动本地 Hardhat 网络
yarn chain

# 3. 部署合约 (新终端)
yarn deploy

# 4. 启动 Relayer 后端 (新终端)
cd relayer
npm install && npx prisma generate
npm run dev

# 5. 启动 Telegram Bot (新终端，可选)
cd telegram-bot
npm install
npm run db:generate && npm run db:push
npm run dev

# 6. 启动前端 (新终端)
yarn start
```

访问 `http://localhost:3000`

### 配置 Privy (社交登录)

1. 访问 https://dashboard.privy.io 创建应用
2. 复制 App ID 到 `packages/nextjs/.env.local`:
   ```bash
   NEXT_PUBLIC_PRIVY_APP_ID=your-app-id-here
   ```
3. 重启前端服务即可使用社交登录

## 🧪 测试

```bash
# 运行所有测试
yarn hardhat:test

# 查看测试覆盖率
yarn hardhat:coverage

# 查看Gas报告
REPORT_GAS=true yarn hardhat:test
```

### 测试结果

```
✔ 38 passing (845ms)
✗ 0 failing
```

| 测试类别 | 测试数量 | 状态 |
|----------|----------|------|
| SidebetFactory | 4 | ✅ |
| Market Creation | 4 | ✅ |
| Staking | 5 | ✅ |
| Proposal | 3 | ✅ |
| Consensus & Finalization | 7 | ✅ |
| Dispute | 3 | ✅ |
| Cancellation | 4 | ✅ |
| Progress Tracking | 2 | ✅ |
| MockToken | 4 | ✅ |

## 📊 Gas 成本

| 操作 | Gas 消耗 |
|------|----------|
| `createSidebet()` | ~2,010,000 |
| `stake()` | ~160,000 |
| `proposeResult()` | ~161,000 |
| `finalizeWithConsensus()` | ~294,000 |
| `dispute()` | ~37,000 |
| `cancel()` | ~68,000 |

## 🔧 开发指南

### 添加新市场

```typescript
// 通过工厂创建市场
const tx = await factory.createSidebet(
  "BTC能否突破10万美元?",      // topic
  6000,                       // 60% 阈值
  usdcAddress,                // 代币地址
  ethers.parseEther("100")    // 最小投注
);
```

### 参与投注

```typescript
// 用户投注
await sidebet.connect(user).stake(
  ethers.parseEther("200"),  // 金额
  1                           // 投"是"
);
```

### 提交结果提案

```typescript
// 任何人都可以提案
await sidebet.connect(anyone).proposeResult(
  1,                           // 结果
  ethers.encodeBytes32String("证据IPFS哈希")
);
```

### 签名认证 (EIP-712)

```typescript
const domain = {
  name: "Sidebet",
  version: "1",
  chainId: chainId,
  verifyingContract: marketAddress,
};

const types = {
  Attestation: [
    { name: "market", type: "address" },
    { name: "outcome", type: "uint256" },
    { name: "nonce", type: "uint256" },
  ],
};

const value = {
  market: marketAddress,
  outcome: 1,
  nonce: await sidebet.nonce(),
};

// 使用 Privy 或钱包签名
const signature = await signer.signTypedData(domain, types, value);
```

### Relayer API 使用

```bash
# 健康检查
curl http://localhost:3001/health

# 获取市场列表
curl http://localhost:3001/api/markets

# 获取市场详情
curl http://localhost:3001/api/markets/:address

# 提交签名
curl -X POST http://localhost:3001/api/attestations \
  -H "Content-Type: application/json" \
  -d '{
    "market": "0x...",
    "signer": "0x...",
    "outcome": 1,
    "signature": "0x...",
    "nonce": 1
  }'
```

### Telegram Bot 配置

1. 与 @BotFather 对话创建 Bot，获取 Token
2. 配置 `telegram-bot/.env`:
   ```bash
   BOT_TOKEN=your-bot-token-here
   RELAYER_API_URL=http://localhost:3001
   MINI_APP_URL=http://localhost:3000/tg
   ```
3. 启动 Bot: `npm run dev`

## 📊 项目完成度

| 模块 | 完成度 | 状态 |
|------|--------|------|
| 智能合约层 | 100% | ✅ |
| 合约测试 | 100% | ✅ (38个测试通过) |
| 前端 UI | 100% | ✅ |
| EIP-712 签名 | 100% | ✅ |
| API 路由 | 100% | ✅ |
| Privy 集成 | 100% | ✅ |
| Relayer 后端 | 100% | ✅ |
| Telegram Bot | 100% | ✅ |
| Monad 部署 | 0% | ⏳ 待部署 |

详细开发清单请查看 [docs/TODO.md](docs/TODO.md)

## 🔐 安全考虑

- **重放攻击防护**: Domain Separator + Nonce
- **重入攻击防护**: OpenZeppelin ReentrancyGuard
- **整数溢出防护**: Solidity 0.8+ 内置检查
- **争议机制**: 2小时争议窗口
- **合约检查**: 禁止合约地址参与

## 📄 许可证

MIT

## 📚 相关文档

- [部署指南](docs/DEPLOYMENT.md) - 完整的生产环境部署文档
- [Monad 部署](docs/MONAD_DEPLOYMENT.md) - Monad 网络部署指南
- [私钥配置](docs/PRIVATE_KEY_SETUP.md) - 加密私钥配置说明
- [开发清单](docs/TODO.md) - 项目进度与待办事项
- [Relayer 文档](relayer/README.md) - 后端服务文档
- [Telegram Bot 文档](telegram-bot/README.md) - Bot 服务文档

## 🙏 致谢

- [Scaffold-ETH 2](https://github.com/scaffold-eth/scaffold-eth-2) - 开发框架
- [OpenZeppelin](https://openzeppelin.com/) - 安全合约库
- [Monad](https://monad.xyz/) - 高性能 Layer1
- [Privy](https://www.privy.io/) - 嵌入式钱包与社交登录
- [grammY](https://grammy.dev/) - Telegram Bot 框架
- [Vercel](https://vercel.com/) - 前端托管平台
- [Railway](https://railway.app/) - 后端托管平台
